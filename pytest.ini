[pytest]
testpaths = tests
markers =
    customers: Tests for Customer API
    payment_intents: Tests for Payment Intent API
    negative: Negative and input validation tests
    smoke: Quick smoke tests
addopts = -v --tb=short
